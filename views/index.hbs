<ul class="tweets">
  {{#each tweets}}
    <li class="tweet">
      <a href="{{this.link}}" class="tweet__message">
        {{this.message}}
      </a>

      <a href="{{this.site}}" class="tweet__handle">
        &mdash; {{this.handle}}
      </a>
    </li>
  {{/each}}
</ul>
